[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    integration: needs Postgres/Chroma/Redis or a live model key
filterwarnings =
    ignore::DeprecationWarning
